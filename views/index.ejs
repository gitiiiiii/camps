<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title || "Home - College Transport" %></title>
    <link rel="stylesheet" href="/css/styles.css" />
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdn.jsdelivr.net/npm/feather-icons/dist/feather.min.js"></script>
</head>
<body class="bg-gray-50 font-sans">

  <!-- Include header -->
  <%- include('partials/header', { user: currentUser, page: 'home' }) %>

  <!-- HERO SECTION -->
  <section id="hero" class="relative bg-blue-600 text-white py-16 text-center">
    <div class="max-w-6xl mx-auto px-6">
      <h1 class="text-5xl font-extrabold mb-4">
        Welcome to <span class="text-blue-200">College Transport System</span>
      </h1>
      <p class="text-blue-100 text-lg mb-6">
        View routes, schedules, and track buses in real-time.
      </p>
      <% if (currentUser) { %>
        <p class="text-sm text-blue-100 mb-4">
          Logged in as: <strong><%= currentUser.name %> (<%= currentUser.role %>)</strong>
        </p>
      <% } %>
      <div class="flex justify-center gap-4">
        <a href="/routes" class="bg-white text-blue-700 px-5 py-3 rounded-md font-medium hover:bg-blue-50">View Routes</a>
        <a href="/schedules" class="bg-blue-800 text-white px-5 py-3 rounded-md font-medium hover:bg-blue-900">View Schedule</a>
        <a href="/tracking" class="bg-white text-blue-700 px-5 py-3 rounded-md font-medium hover:bg-blue-50">Track Bus</a>
      </div>
    </div>
  </section>

  <!-- FEATURES SECTION -->
  <section class="py-16 bg-white">
    <div class="max-w-7xl mx-auto px-6 text-center">
      <h2 class="text-blue-600 font-semibold uppercase">Features</h2>
      <h3 class="text-3xl font-extrabold text-gray-900 mb-12">
        A Better Way to Navigate Campus
      </h3>

      <div class="grid md:grid-cols-2 gap-8">
        <div class="p-6 border rounded-lg hover:shadow-lg transition">
          <i data-feather="map" class="text-blue-500 w-10 h-10 mx-auto mb-4"></i>
          <h4 class="font-semibold text-lg mb-2">Live Bus Tracking</h4>
          <p class="text-gray-600">Track bus locations in real-time using GPS-enabled updates.</p>
        </div>

        <div class="p-6 border rounded-lg hover:shadow-lg transition">
          <i data-feather="calendar" class="text-blue-500 w-10 h-10 mx-auto mb-4"></i>
          <h4 class="font-semibold text-lg mb-2">Dynamic Scheduling</h4>
          <p class="text-gray-600">Up-to-date bus schedules that adjust based on route traffic.</p>
        </div>

        <div class="p-6 border rounded-lg hover:shadow-lg transition">
          <i data-feather="users" class="text-blue-500 w-10 h-10 mx-auto mb-4"></i>
          <h4 class="font-semibold text-lg mb-2">Role-Based Login</h4>
          <p class="text-gray-600">Different dashboards for students, faculty, drivers, and admin.</p>
        </div>

        <div class="p-6 border rounded-lg hover:shadow-lg transition">
          <i data-feather="bell" class="text-blue-500 w-10 h-10 mx-auto mb-4"></i>
          <h4 class="font-semibold text-lg mb-2">Smart Notifications</h4>
          <p class="text-gray-600">Instant alerts for delays, route changes, and arrivals.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA SECTION -->
  <section class="bg-blue-600 text-white py-16 text-center">
    <h2 class="text-3xl font-bold mb-4">Ready to start your journey?</h2>
    <p class="text-blue-200 mb-6">Register now and experience smart, hassle-free transport.</p>
    <div class="flex justify-center gap-4">
      <a href="/register" class="bg-white text-blue-700 px-6 py-3 rounded-md font-medium hover:bg-blue-50">Register</a>
      <a href="/login" class="bg-blue-800 text-white px-6 py-3 rounded-md font-medium hover:bg-blue-900">Login</a>
    </div>
  </section>

  <!-- Include footer -->
  <%- include('partials/footer') %>

  <script> feather.replace(); </script>
</body>
</html>
